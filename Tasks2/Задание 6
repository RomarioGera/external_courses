Написать функцию, которая принимает на входе любое число, но не больше 1000 и определяет является ли оно простым и выводит простое число или нет. Если введено больше 1000, то выводится, что данные неверны.
Вариант 1:
var input = prompt("Введите число от 0 до 1000");
    if (input > 1000 ) {
        alert("Вы ввели некорректное число. Обновите страницу и попробуйте ещё раз.");        
    } 
    else {for (var i=2;i<=1000;i++){
        var j=1;
         if(input>2 && input%j==0 && input%i!==0){
          var result = input+" простое";
       } else{
      var result = input +" составное"; 
      }
    } alert(result);
}

Вариант 2:
var input = prompt("Введите число от 0 до 1000");
    if (input > 100 ) {
        alert("Вы ввели некорректное число. Обновите страницу и попробуйте ещё раз.");        
    } 
    else {var input2 = function isPrime(input) {
        if(input < 2) {alert("Вы ввели составное число");}
        for (var i = 2; i < input; i++) {
            if(input%i==0)
            {alert("Вы ввели составное число");}
        }
        alert("Вы ввели простое число");};
}
